// Suppression des scripts inutiles, minification, lazy loading JS si nécessaire

// Lazy loading polyfill pour anciens navigateurs
if('loading'in HTMLImageElement.prototype){}else{document.querySelectorAll('img[loading="lazy"]').forEach(function(img){img.src=img.dataset.src;});}